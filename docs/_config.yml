title: VM RSync
description: Bidirectional file sync between a local workspace and remote machines via rsync over SSH
url: "https://carlosrabelo.github.io"
baseurl: "/vmrsync"
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
markdown: kramdown
exclude:
  - Gemfile
  - Gemfile.lock
